import { Button, Flex, Image } from '@tencent/comby-lib-mobile'

const button = new Button(Image)
const panel = new Panel(Button, Flex)

function render () {
  const Flex = function () {}

  const btn2 = new Button(Image)
  const panel2 = new Panel(Button, Flex)
}
