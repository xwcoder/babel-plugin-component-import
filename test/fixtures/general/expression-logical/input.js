import { Button, Text, Flex } from '@tencent/comby-lib-mobile'

const ui = Button || Text || Flex

function render () {
  const Text = function () {}
  const ui = Button || Text
}

const { a } = Flex
