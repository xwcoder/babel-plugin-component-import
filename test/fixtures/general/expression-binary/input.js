import { Button } from '@tencent/comby-lib-mobile'
const extraProps = undefined === Button ? { type: 'primary' } : {}
console.log(extraProps)

function render () {
  const Button = 1

  const isButton = Button === 1
}
