import { Button, Feed } from '@tencent/comby-lib-mobile'
import comby from '@tencent/comby-lib-mobile'

let myButton = null

myButton = Button
myButton = comby.Button
// myButton = comby

console.log(myButton)

const { ui10, ui11, ...uiRest } = Feed
