import { Button } from '@tencent/comby-lib-mobile'

ReactDOM.render(<div component={Button} />)

const coms = {
  button: Button
}

const btns = {
  Button
}

function render () {

  const Button = function () {}

  const cache = {
    btn: Button
  }
}
